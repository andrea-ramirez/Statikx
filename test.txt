script example02; 
DO
{ 
    var int -> x;
    var float -> y;
    var int -> d;
    var char -> z;
    var char -> we;

    put(x / 2);
    y = x * x + x / 2;
    put("Esto es un letrero");
    we = 'c';
    x = 2;
    y = 3.0;

    if(z == we)
    True{
        put("x es mayor");
    }False{
        put("y es mayor");
    }
}