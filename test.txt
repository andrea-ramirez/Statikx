script example02; 
DO
{ 
    var int -> x;
    var float -> y;
    var int -> d;
    var char -> z;
    var char -> we;

    put(y);
    y = x * x + x / 2;
    put("Esto es un letrero");
    mode(x);
    y = x + 5.0;
    mean(y);
    variance(y);
    we = 'c';
}